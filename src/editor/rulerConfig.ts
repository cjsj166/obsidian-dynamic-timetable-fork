// Shared ruler geometry constants.

/** Minimum pixels per scheduled hour. Blocks reserve space to guarantee it, so
 *  every hour tick stays visible no matter how short the memo is. */
export const MIN_HOUR_PX = 22;
